const Utils = { Storage: { get(key, defaultValue = null) { try { return JSON.parse(localStorage.getItem(`skillbridge_${key}`)) || defaultValue } catch { return defaultValue } }, set(key, value) { try { localStorage.setItem(`skillbridge_${key}`, JSON.stringify(value)); return true } catch { return false } }, remove(key) { localStorage.removeItem(`skillbridge_${key}`) } }, Date: { timeAgo(date) { const diff = Math.floor((new Date() - new Date(date)) / 1000); if (diff < 60) return 'just now'; if (diff < 3600) return Math.floor(diff / 60) + 'm ago'; if (diff < 86400) return Math.floor(diff / 3600) + 'h ago'; return Math.floor(diff / 86400) + 'd ago' }, formatDate(date, format = 'MMM DD, YYYY') { const d = new Date(date), months = ['Jan', 'Feb', 'Mar', 'Apr', 'May', 'Jun', 'Jul', 'Aug', 'Sep', 'Oct', 'Nov', 'Dec']; return format.replace('YYYY', d.getFullYear()).replace('MMM', months[d.getMonth()]).replace('DD', String(d.getDate()).padStart(2, '0')) } }, String: { truncate(str, len = 100) { return str.length > len ? str.substring(0, len) + '...' : str }, capitalize(str) { return str.charAt(0).toUpperCase() + str.slice(1).toLowerCase() } }, Number: { formatCompact(num) { if (num >= 1000000) return (num / 1000000).toFixed(1) + 'M'; if (num >= 1000) return (num / 1000).toFixed(1) + 'K'; return num.toString() }, formatCurrency(num) { return new Intl.NumberFormat('en-US', { style: 'currency', currency: 'USD' }).format(num) } }, Validation: { email(email) { return /^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email) }, password(password) { return { isValid: password.length >= 6, strength: password.length >= 10 ? 'Strong' : password.length >= 6 ? 'Medium' : 'Weak' } } } };