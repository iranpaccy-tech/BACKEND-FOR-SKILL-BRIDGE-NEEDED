const Marketplace = { listings: [{ id: 1, user: 'John Doe', skill: 'React Development', lookingFor: 'Python', type: 'skill-for-skill' }, { id: 2, user: 'Sarah Parker', skill: 'UI/UX Design', lookingFor: 'JavaScript', type: 'mentorship' }], init() { this.loadListings() }, loadListings() { const container = document.getElementById('marketplaceGrid'); if (!container) return; container.innerHTML = this.listings.map(l => `<div class="listing-card"><h3>${l.skill}</h3><p>Offered by: ${l.user}</p><p>Looking for: ${l.lookingFor}</p><button class="btn-outline" onclick="Marketplace.contact(${l.id})">Connect</button></div>`).join('') }, contact(id) { if (!Auth.isAuthenticated()) { Auth.showLogin(); return } UI.showToast('Connection request sent!', 'success') } }; document.addEventListener('DOMContentLoaded', () => { if (window.location.pathname.includes('marketplace.html')) Marketplace.init() });