const Certificates = { list: [{ id: 1, title: 'JavaScript Fundamentals', date: '2026-03-15', credential: 'SB-JS-001' }, { id: 2, title: 'Python Basics', date: '2026-02-28', credential: 'SB-PY-089' }], init() { this.loadCertificates() }, loadCertificates() { const container = document.getElementById('certificatesGrid'); if (!container) return; container.innerHTML = this.list.map(c => `<div class="certificate-card"><h3>${c.title}</h3><p>Issued: ${c.date}</p><p>ID: ${c.credential}</p><button class="btn-outline" onclick="Certificates.download(${c.id})">Download PDF</button><button class="btn-link" onclick="Certificates.share(${c.id})">Share</button></div>`).join('') }, download(id) { UI.showToast('Downloading certificate...', 'info'); setTimeout(() => UI.showToast('Download complete!', 'success'), 1500) }, share(id) { UI.showToast('Certificate link copied!', 'success') } }; document.addEventListener('DOMContentLoaded', () => { if (window.location.pathname.includes('certificates.html')) Certificates.init() });