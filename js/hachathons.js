const Hackathons = { events: [{ id: 1, title: 'AI Innovation Challenge', prize: 10000, deadline: '2026-03-20', teams: 342 }, { id: 2, title: 'Web3 Buildathon', prize: 15000, deadline: '2026-03-27', teams: 156 }], init() { this.loadEvents() }, loadEvents() { const container = document.getElementById('hackathonsGrid'); if (!container) return; container.innerHTML = this.events.map(e => `<div class="hackathon-card"><h3>${e.title}</h3><p>🏆 $${e.prize} • 👥 ${e.teams} teams</p><p>⏰ Deadline: ${e.deadline}</p><button class="btn-primary" onclick="Hackathons.register(${e.id})">Register</button></div>`).join('') }, register(id) { if (!Auth.isAuthenticated()) { Auth.showLogin(); return } UI.showToast('Registration successful!', 'success') } }; document.addEventListener('DOMContentLoaded', () => { if (window.location.pathname.includes('hackathons.html')) Hackathons.init() });