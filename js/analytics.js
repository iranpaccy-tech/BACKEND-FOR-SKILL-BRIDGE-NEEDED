const Analytics = { data: { hours: 127, courses: 8, skills: 12, streak: 15, weekly: [2, 3, 4, 2, 5, 6, 4] }, init() { this.updateStats() }, updateStats() { document.getElementById('totalHours').textContent = this.data.hours; document.getElementById('coursesCompleted').textContent = this.data.courses; document.getElementById('skillsMastered').textContent = this.data.skills; document.getElementById('streakDays').textContent = this.data.streak }, loadChart() { console.log('Chart would render here') } }; document.addEventListener('DOMContentLoaded', () => { if (window.location.pathname.includes('analytics.html')) Analytics.init() });